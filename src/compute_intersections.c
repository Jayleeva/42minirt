#include "../inc/minirt.h"

int compute_intersections(t_data *data)
{
	int	x;
	int	y;

	x = 0;
	while (x < W_WIDTH)
	{
		y = 0;
		while (y < W_HEIGHT)
		{
			(void)data;
			y ++;
		}
		x ++;
	}
	return (1);
}